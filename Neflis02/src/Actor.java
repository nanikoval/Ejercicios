import java.util.function.Predicate;

public class Actor {

    //punto 5a
    /*public Actor actoresQueActuaron(){
        return contenidosVistos.stream().map(actor->actor).equals(actores)
    }

    public Actor estaEnUnContenido (Actor unActor){
        return actoresQueActuaron().stream().filter(actor->actor.equals(this));
    }*/
}
